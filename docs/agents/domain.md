# Domain docs

Single-context repo. The ubiquitous language lives in `CONTEXT.md` at the root; architectural decisions live in `docs/adr/`.

Consumer rules: read `CONTEXT.md` before naming anything; use its terms and avoid the listed alternatives. Read the ADRs touching the area you change; do not reverse one without writing a superseding ADR.
